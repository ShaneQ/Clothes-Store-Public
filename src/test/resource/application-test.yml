server: 
  port: 8081
  servlet: 
    context-path: /resource-server

logging:
  level:
    org:
      springframework:
        security: DEBUG
        spring:
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://dev-.....okta.com/oauth2/default

  flyway:
    enabled: false